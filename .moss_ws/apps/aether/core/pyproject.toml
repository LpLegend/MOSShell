[project]
name = "aether_core"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "ghoshell-moss[host]",
    "aiohttp>=3.9",
]

[tool.uv]
override-dependencies = [
    "numpy<2.3",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }
