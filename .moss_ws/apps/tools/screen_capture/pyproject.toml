[project]
name = "moss-tool-screen-capture"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "mss>=9.0.0",
    "ghoshell-moss[host]",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }
