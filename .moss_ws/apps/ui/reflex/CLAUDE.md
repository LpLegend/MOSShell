# MOSS Start (MANDATORY)

At the beginning of EVERY conversation, BEFORE any other tool calls, file reads, or responses, run:

```
moss start
```

This is a blocking prerequisite. The output is the MOSS cognitive map — required context for all work in this project. Do not skip it.
