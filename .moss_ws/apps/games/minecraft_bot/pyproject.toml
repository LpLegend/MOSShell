[project]
name = "minecraft-bot"
version = "0.1.0"
description = "Minecraft bot as a MOSS App — mineflayer-based channel for Ghost control"
requires-python = ">=3.11"
dependencies = [
    "ghoshell-moss[host]",
    "javascript",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../../", editable = true }
