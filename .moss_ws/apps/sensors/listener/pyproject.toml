[project]
name = "listener"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
    "ghoshell-moss[host]",
    "scipy==1.17.1",
]

[tool.uv]
override-dependencies = [
    "numpy<2.3",
]

[tool.uv.sources]
ghoshell-moss = { path = "../../../..", editable = true }
