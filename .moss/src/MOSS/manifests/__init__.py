# MOSS Matrix Manifests — global capability declarations.
#
# Each sub-package declares one kind of capability:
#   providers/  — IoC dependency injection factories
#   configs/    — configuration models with defaults
#   signals/    — signal protocol declarations
#   topics/     — event topic protocol declarations
#   parameters/ — parameter protocol declarations
#   resources/  — resource storage declarations
#
# Discovered at runtime by ScannedMatrixManifest.
#
# --
# MOSS 矩阵 Manifest — 全局能力声明。
# 每个子包声明一种能力类型，Matrix 启动时自动扫描发现。
