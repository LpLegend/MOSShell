# Mode Resource manifest — mode-specific resource storage declarations.
#
# Extends global resources (MOSS.manifests.resources) with mode-specific datasets.
# Define ResourceStorageMeta instances here.
#
# --
# Mode Resource 清单 — mode 专属资源存储声明。
# 扩展全局资源，在此定义 mode 专属的 ResourceStorageMeta 实例。
