# Mode Signal manifest — mode-specific signal protocol declarations.
#
# Extends global signals (MOSS.manifests.signals) with mode-specific types.
# Define SignalMeta subclasses or SignalSchema instances here.
#
# --
# Mode Signal 清单 — mode 专属 Signal 声明。
# 扩展全局 Signal，在此定义 mode 专属的 SignalMeta 子类或 SignalSchema 实例。
