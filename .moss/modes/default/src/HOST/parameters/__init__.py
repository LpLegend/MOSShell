# Mode Parameter manifest — mode-specific parameter protocol declarations.
#
# Extends global parameters (MOSS.manifests.parameters) with mode-specific types.
# Define ParameterModel subclasses or ParameterSchema instances here.
#
# --
# Mode Parameter 清单 — mode 专属 Parameter 声明。
# 扩展全局 Parameter，在此定义 mode 专属的 ParameterModel 子类或 ParameterSchema 实例。
